<h1>タスクリスト一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  <li> <%= link_to task.id,task %>:<%= task.content %> -&gt; ステータス:<%= task.status %></li>
  <% end %>
</ul>

<%= link_to 'タスク登録',new_task_path %>